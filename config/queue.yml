# config/queue.yml
development:
  adapter: solid_queue
  dispatcher:
    polling_interval: 1
  worker:
    polling_interval: 1
    threads: 5

test:
  adapter: solid_queue
  dispatcher:
    polling_interval: 1
  worker:
    polling_interval: 1
    threads: 5

production:
  adapter: solid_queue
  dispatcher:
    polling_interval: 1
  worker:
    polling_interval: 1
    threads: 5